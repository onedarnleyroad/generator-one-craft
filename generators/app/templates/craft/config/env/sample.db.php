<?php
/**
 * Environment-specific Database Configuration
 *
 * The returned array will be merged with the CRAFT_ENVIRONMENT key's values in config/db.php.
 *
 * Usage: duplicate file as `config/env/db.php` and cutomise values below.
 */

return array(
    'server'    => 'localhost',
    'user'      => 'root',
    'password'  => 'root',
    'database'  => ''
);
